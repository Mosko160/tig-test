let
    var condition := 0
in
    for i := "a" to 2 do
        (condition := condition + 1);
    print_int(condition)
end
/*
FIXME: error message

wrong_init_type.tig:4.9: type mismatch
  variable type: string
  expected type: int
*/
