let
    var condition := 0
in
    for i := 1 to nil do
        (condition := condition + 1);
    print_int(condition)
end
/*
FIXME: error message
wrong_border_type.tig:4.19-21: type mismatch
  upper bound type: nil
  expected type: int
*/
